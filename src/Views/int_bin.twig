#!/usr/bin/env php
<?php
require __DIR__ . "/vendor/autoload.php";

define("BASE_PATH", __DIR__);

define("CONSOLE_NAME", "{{ bin }}");

date_default_timezone_set("Asia/Shanghai");

$app = new \{{ namespace }}\{{ app_class_name }};

$app->handle();